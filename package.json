{
  "name": "gates-of-atlantis",
  "version": "0.1",
  "description": "a ship-based cRPG",
  "author": "NoxZet",
  "license": "MIT",
  "scripts": {
    "run": "node server/main_server.js",
    "run-dev": "node --enable-source-maps server/main_server.js",
    "build-dev": "webpack --mode development",
    "build": "webpack --mode production",
    "start": "npm run build && npm run run",
    "start-dev": "npm run build-dev && npm run run-dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/ws": "^8.5.10",
    "bufferutil": "^4.0.8",
    "ts-loader": "^9.5.1",
    "typescript": "^5.3.3",
    "utf-8-validate": "^6.0.3",
    "webpack-cli": "^5.1.4",
    "ws": "^8.16.0"
  }
}
